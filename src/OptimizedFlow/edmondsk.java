package OptimizedFlow;

import ADTGraph.Graph;
import ADTGraph.Vertex;

/**
 * Created by remen on 13.12.15.
 */
public class edmondsk {

    /**
     * Uses the edmonds & karp algorithm to calculate the value of the
     * greatest flow.
     *
     *
     */
    public static int edmondskarp(Graph graph, Vertex source, Vertex target) {

        return 0;
    }

    /**
     * Like {@link edmondsk#edmondskarp(Graph, Vertex, Vertex)}, but returns the
     * time needed for this algorithm.
     */
    public static long edmondskarpRtm(Graph graph, Vertex source, Vertex target) {
        return 0;
    }

    /**
     * Like {@link edmondsk#edmondskarp(Graph, Vertex, Vertex)}, but returns the
     * amount of accesses on the graph.
     */
    public static long edmondskarpAcc(Graph graph, Vertex source, Vertex target) {
        return 0;
    }
}
